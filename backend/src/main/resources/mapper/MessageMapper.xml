<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiagent.mapper.MessageMapper">

    <resultMap id="BaseResultMap" type="com.aiagent.model.Message">
        <id column="id" property="id"/>
        <result column="conversation_id" property="conversationId"/>
        <result column="role" property="role"/>
        <result column="content" property="content"/>
        <result column="model" property="model"/>
        <result column="tokens" property="tokens"/>
        <result column="response_time" property="responseTime"/>
        <result column="created_at" property="createdAt"/>
        <result column="deleted_at" property="deletedAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.aiagent.model.Message">
        INSERT INTO messages (id, conversation_id, role, content, model, tokens, response_time, created_at)
        VALUES (#{id}, #{conversationId}, #{role}, #{content}, #{model}, #{tokens}, #{responseTime}, #{createdAt})
    </insert>

    <select id="selectById" parameterType="string" resultMap="BaseResultMap">
        SELECT * FROM messages
        WHERE id = #{id} AND deleted_at IS NULL
    </select>

    <select id="selectByConversationId" parameterType="string" resultMap="BaseResultMap">
        SELECT * FROM messages
        WHERE conversation_id = #{conversationId} AND deleted_at IS NULL
        ORDER BY created_at ASC
    </select>

    <update id="deleteById" parameterType="string">
        UPDATE messages
        SET deleted_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

</mapper>

